# Makefile for school executable with CC and SRC as the variables
# CC - the compiler to be used
# SR - the .c files

CC = gcc
SRC = main.c student.c 

school: $(SRC:.c=.o)
    $(CC) $(SRC:.c=.o) -o school

%.o: %.c
    $(CC) -c $<

all: school

clean:
    rm -f *.o school

