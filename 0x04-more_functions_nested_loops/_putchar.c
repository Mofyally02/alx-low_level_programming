#include <unistd.h>
#include "main.h"

/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 * Return:On success l else -l 
 */
int _putchar(char c)
{
	return (write(l, &c, l));
}
